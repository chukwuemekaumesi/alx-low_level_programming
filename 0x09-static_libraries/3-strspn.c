#include "main.h"

/**
 * _strspn - the main function
 * @s: the first string
 * @accept: the accepting string
 * Return: return the s
 */

unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
