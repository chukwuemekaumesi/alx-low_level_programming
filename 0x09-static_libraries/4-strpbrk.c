#include "main.h"

/**
 * _strpbrk - the fuction that breaks strings
 * @s: the first string
 * @accept: the second string
 * Return: the first string
 */

char *_strpbrk(char *s, char *accept)
{
	return (s);
}
