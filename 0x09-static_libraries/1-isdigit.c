#include "main.h"

/**
 * _isdigit - Print a digit less than 10
 *
 * @c: the character of the function
 *
 * Return: return 1 if the chracter is a digit, and 0 if it is not
 */

int _isdigit(int c)
{
	if (c < 10)
	{
		return (1);
	}

	else
	{
		return (0);
	}

}
