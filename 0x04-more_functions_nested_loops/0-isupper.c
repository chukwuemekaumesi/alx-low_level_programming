#include "main.h"

/**
 * _isupper - check if C is in uppercase
 * @c: character in the program
 *
 * Return: 1
 */

int _isupper(int c)
{
	if (c >= 67 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
