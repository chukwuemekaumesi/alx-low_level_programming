#include <stdio.h>

/**
 * main - prints out the number of commands
 * @argc: the number of commands it takes
 * @argv: the array string
 *
 * Return: 0 if successful
 */

int main(int argc, char *argv[]__attribute__((unused)))
{
	printf("%d\n", argc);
	return (0);
}
